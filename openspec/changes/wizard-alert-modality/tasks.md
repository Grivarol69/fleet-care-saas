# Tasks: Wizard Alert Modality — Modalidad y Proveedor en OTs

## Phase 1: Schema & Migration

- [ ] 1.1 In `prisma/schema.prisma`, add `providerId String?` field and `provider Provider? @relation(fields: [providerId], references: [id])` relation to the `WorkOrderItem` model (after the `supplier String` field), and add `@@index([providerId])` at the bottom of the model's index list
- [ ] 1.2 Run `npx prisma migrate dev --name add_provider_to_work_order_item` to generate the migration and update the Prisma client. Verify the migration file appears in `prisma/migrations/`

---

## Phase 2: Backend

- [ ] 2.1 In `src/app/api/maintenance/work-orders/[id]/items/[itemId]/route.ts`, add `providerId: z.string().nullable().optional()` to the `updateItemSchema` Zod object
- [ ] 2.2 In the same file, extend the `updateData` type to include `providerId?: string | null`, and add a block `if (updates.providerId !== undefined) { updateData.providerId = updates.providerId; }` after the existing field assignments
- [ ] 2.3 In the same file, add `provider: { select: { id: true, name: true } }` to the `include` object of the `tenantPrisma.workOrderItem.update(...)` call so the response includes the provider shape
- [ ] 2.4 In the same file, add `providerId: updatedItem.providerId` and `provider: updatedItem.provider ? { id: updatedItem.provider.id, name: updatedItem.provider.name } : null` to the `item` object inside the `NextResponse.json` return
- [ ] 2.5 Create new file `src/app/api/maintenance/work-orders/[id]/purchase-orders/route.ts` with a `POST` handler that: (a) authenticates with `requireCurrentUser`, (b) validates `canExecuteWorkOrders`, (c) parses body `{ itemIds: string[] }` with Zod (non-empty array), (d) fetches all WorkOrderItems for those IDs including `providerId`, `purchaseOrderItems { select: { id: true } }`, and `mantItem { select: { type: true } }`, (e) validates all items have `providerId` (return 400 "Todos los ítems deben tener proveedor asignado"), (f) validates no item has existing PurchaseOrderItems (return 400), (g) groups items by `providerId`, (h) for each group uses `$transaction` to count existing PurchaseOrders for the tenant in the current year, generates `orderNumber` as `OC-{YEAR}-{count+1 padded to 6 digits}`, creates one `PurchaseOrder` with `status: 'DRAFT'`, `type: allParts ? 'PARTS' : 'SERVICES'`, and creates corresponding `PurchaseOrderItem` rows, (i) after all groups: `updateMany` the processed WorkOrderItems to `closureType: 'PURCHASE_ORDER'`, (j) returns 201 with array of created PurchaseOrders
- [ ] 2.6 In `src/app/api/maintenance/work-orders/route.ts` (POST handler), destructure `modality` from `body` with default `'INTERNAL'`. Update the `workOrderItem.create` call inside the `alerts.map(...)` block: when `modality === 'INTERNAL'` set `itemSource: 'INTERNAL_STOCK'`; when `modality === 'EXTERNAL'` set `itemSource: 'EXTERNAL'`. In both cases `providerId: null` — providers are assigned post-creation. **Do NOT set `WorkOrder.providerId`**
- [ ] 2.7 In `src/app/api/maintenance/work-orders/[id]/route.ts` (GET handler), in the `workOrderItems` include block add: `providerId: true`, `provider: { select: { id: true, name: true } }`, and `purchaseOrderItems: { select: { id: true } }` alongside the existing `mantItem` include

---

## Phase 3: Frontend

- [ ] 3.1 In `src/app/dashboard/maintenance/work-orders/[id]/page.tsx`, extend the `WorkOrder.workOrderItems` array type to add: `providerId: string | null`, `provider: { id: string; name: string } | null`, and `purchaseOrderItems: Array<{ id: string }>`. Also extend the `purchaseOrders` array type to include `type: string`, `subtotal: number`, and `total: number` (align with actual API shape from phase 2.7)
- [ ] 3.2 In `src/app/dashboard/maintenance/work-orders/components/WorkOrderDetail/WorkItemRow.tsx`, add props `providers?: Array<{ id: string; name: string }>` and extend the `item` type used by the component to include `providerId: string | null`, `provider: { id: string; name: string } | null`, and `purchaseOrderItems: Array<{ id: string }>`. Derive `hasActivePO = (item.purchaseOrderItems?.length ?? 0) > 0`
- [ ] 3.3 In `WorkItemRow.tsx`, add a modality toggle button: visible at all times in the row (not only when expanded). When `item.itemSource === 'INTERNAL_STOCK'` show a clickable badge "Taller Propio" — clicking it calls PATCH with `{ itemSource: 'EXTERNAL', providerId: null }`. When `item.itemSource === 'EXTERNAL'` show a clickable badge "Externo" — clicking calls PATCH with `{ itemSource: 'INTERNAL_STOCK', providerId: null }`. On click, perform optimistic hide (local state `hidden`) then call `onRefresh()` on success. Wrap with `Tooltip` showing "Tiene ticket interno activo — no se puede mover" when the item has an active InternalWorkTicket (check via `item.internalWorkTickets` if available in type, otherwise disable if `item.itemSource === 'INTERNAL_STOCK'` and a flag `hasActiveTicket` is passed from the tab). Disable toggle when `hasActivePO` is true and show tooltip "Ya tiene OC generada — cancelar la OC primero"
- [ ] 3.4 In `WorkItemRow.tsx`, add an inline provider display and selector for EXTERNAL items: display `provider.name ?? supplier ?? '—'` as a text label. When `item.itemSource === 'EXTERNAL'` and `providers` prop is provided and `!hasActivePO`, render a `Select` with the `providers` list. On change, call PATCH `{ providerId: selectedId }` then `onRefresh()`. When `item.itemSource === 'EXTERNAL'` and `providerId === null`, show a badge "Sin proveedor" (orange/warning color)
- [ ] 3.5 In `src/app/dashboard/maintenance/work-orders/components/WorkOrderDetail/TrabajosExternosTab.tsx`, add a `useEffect` on mount that fetches `GET /api/people/providers` and stores the result in local state `providers: Array<{ id: string; name: string }>`. Pass `providers={providers}` to every `<WorkItemRow>` rendered in this tab
- [ ] 3.6 In `TrabajosExternosTab.tsx`, add a "Mismo proveedor para todos" checkbox section above the items cards. When checked: show a provider `Select` and an "Aplicar a todos" button. Clicking "Aplicar a todos" sends a PATCH `{ providerId }` for each external item that does NOT have `hasActivePO`, sequentially or in parallel, then calls `onRefresh()`. Show a loading state during the batch
- [ ] 3.7 In `TrabajosExternosTab.tsx`, update the checkbox `disabled` logic per item: disable the selection checkbox when `item.closureType !== 'PENDING'` (already done) OR when `item.providerId === null`. Ensure items with `providerId === null` show a visible "Sin proveedor" indicator (already handled by WorkItemRow in task 3.4, but confirm the Checkbox has a tooltip "Asigna un proveedor primero")
- [ ] 3.8 In `src/app/dashboard/maintenance/work-orders/components/WorkOrderDetail/TallerPropioTab.tsx`, verify that `workOrderId` is already passed to `WorkItemRow` (it is per the design doc). Pass `providers={[]}` or omit the prop so the toggle in WorkItemRow renders without a provider dropdown for internal items. No other changes needed in this file beyond ensuring the toggle introduced in task 3.3 is visible
- [ ] 3.9 In `src/components/maintenance/work-orders/WorkOrderCreateWizard.tsx`, add `modality: z.enum(['INTERNAL', 'EXTERNAL']).default('INTERNAL')` to the `formSchema` Zod object. Remove `globalProviderId` and any `providerId` field referencing `WorkOrder.provider` (legacy). No provider fetch needed in the wizard
- [ ] 3.10 In `WorkOrderCreateWizard.tsx`, in **Step 2** (the "resumen" step where selected alerts are listed as a package), add a toggle/radio group "¿Dónde se realizará el trabajo?" with options "Taller Propio" (INTERNAL) and "Servicio Externo" (EXTERNAL). If the wizard currently shows a WorkOrder-level provider dropdown anywhere in Step 2, remove it entirely. Wire the toggle to the `modality` form field. No provider Select in the wizard — providers are assigned post-creation in TrabajosExternosTab
- [ ] 3.11 In `WorkOrderCreateWizard.tsx`, update the `onSubmit` handler to include only `modality` in the POST body (not `globalProviderId`, not `providerId`). Remove any existing provider-related field from the POST payload
- [ ] 3.12 In `src/app/dashboard/maintenance/work-orders/components/WorkOrderDetail/ResumenTab.tsx`, find and remove any display of `workOrder.provider` (the WorkOrder-level provider field). Providers are resolved at item level — this field is legacy and should not appear in the OT summary view
- [ ] 3.13 In `src/app/dashboard/maintenance/work-orders/components/WorkOrderDetail/ComprasTab.tsx`, verify the existing component shows `orderNumber`, provider name, `type`, status badge (with correct colors: DRAFT=yellow/amber, SENT=blue, etc.), and `total`. Add display of `type` badge (SERVICES / PARTS) if not already present. Add empty state "No hay órdenes de compra generadas" if `purchaseOrders.length === 0`

---

## Phase 4: Testing

- [ ] 4.1 In `src/app/api/maintenance/work-orders/[id]/items/__tests__/` (or the existing test file for this route), add an integration test: PATCH `itemId` with `{ providerId: "<validId>" }` → expect 200 and response body contains `providerId` and `provider.name`
- [ ] 4.2 Add integration test: PATCH `itemId` with `{ providerId: null }` → expect 200 and `providerId: null` in response
- [ ] 4.3 Add integration test for `POST /api/maintenance/work-orders/[id]/purchase-orders`: given 2 items with same `providerId` → expect 201 with 1 PurchaseOrder containing 2 items, `orderNumber` matching `OC-YYYY-000001` format, and both items updated to `closureType: PURCHASE_ORDER`
- [ ] 4.4 Add integration test: POST purchase-orders with items from 2 different providers → expect 201 with exactly 2 PurchaseOrders
- [ ] 4.5 Add integration test: POST purchase-orders with an item that has `providerId = null` → expect 400 with message "Todos los ítems deben tener proveedor asignado"
- [ ] 4.6 Add integration test: POST purchase-orders with an item that already has a `PurchaseOrderItem` associated → expect 400
- [ ] 4.7 Add unit test for `orderNumber` generation logic: mock `tenantPrisma.purchaseOrder.count` returning `N` → verify generated number is `OC-{YEAR}-{N+1 zero-padded}`
- [ ] 4.8 Add unit test for provider display fallback: given `{ provider: { name: 'Gomería' }, supplier: 'Old' }` → displays `'Gomería'`; given `{ provider: null, supplier: 'Old' }` → displays `'Old'`; given `{ provider: null, supplier: '' }` → displays `'—'`
- [ ] 4.9 In `src/app/api/maintenance/work-orders/__tests__/` (existing test), add test: POST `/api/maintenance/work-orders` with `modality: 'INTERNAL'` and alert IDs → all created WorkOrderItems have `itemSource: 'INTERNAL_STOCK'` and `providerId: null`
- [ ] 4.10 Add test: POST work-orders with `modality: 'EXTERNAL'` and `globalProviderId: '<id>'` → all items have `itemSource: 'EXTERNAL'` and `providerId: '<id>'`
