# Tasks: Onboarding Seed Improvement & UI Optimization

## Phase 1: Onboarding UX Sync

- [x] 1.1 In `src/actions/onboarding.ts`, remove `orgName` from the `updateTenantProfile` parameters, relying instead on the existing `tenant.name` generated by the JIT creation process.
- [x] 1.2 In `src/app/onboarding/page.tsx`, remove the `<Input id="orgName">` field from the UI. Display the `tenant?.name` as a read-only confirmation or simply hide it, submitting only `country` and `currency`.

## Phase 2: Tenant Seed Overhaul

- [x] 2.1 In `src/actions/seed-tenant.ts`, delete the loops that artificially generate `VehicleBrand`, `VehicleLine`, `VehicleType`, and `MantCategory`. 
- [x] 2.2 Update `src/actions/seed-tenant.ts` to fetch an existing global VehicleBrand, VehicleLine, and a global Maintenance Template (e.g., 'Toyota Hilux Standard'). Use them to insert exactly one dummy `Vehicle` via `tenantPrisma.vehicle.create`.
- [x] 2.3 Create a `VehicleMantProgram` linking the dummy `Vehicle` to the global Maintenance Template so the user sees a fully configured maintenance program upon login.
- [x] 2.4 Update `src/actions/seed-tenant.ts` to create exactly one dummy `Driver`.
- [x] 2.5 Update `src/actions/seed-tenant.ts` to create exactly one dummy `Provider`.

## Phase 3: DataTables Cleanup

> Note: For all lists in `src/app/dashboard/**/*.tsx`.
- [x] 3.1 Remove the `id` object from the `columns: ColumnDef<...>[]` array in `FleetVehiclesList.tsx` and point the filter input to `licensePlate`.
- [x] 3.2 Remove the `id` object from the `columns` array in `BrandList.tsx`, `LineList.tsx`, and `TypeList.tsx` and point the filter input to `name`.
- [x] 3.3 Remove the `id` object from the `columns` array in `DriverList.tsx`, `TechnicianList.tsx`, and `ProviderList.tsx` and point the filter input to `name` or `email` or `documentNumber`.
- [x] 3.4 Remove the `id` object from the `columns` array in `PartsList.tsx`, `VehiclePartsList.tsx`, `CategoriesList.tsx`, and `MantItemsList.tsx` and point the filter to `name` or `description`.
- [x] 3.5 Remove the `id` object from the `columns` array in `PackageList.tsx` and `DocumentTypeList.tsx` and point the filter to `name`.

## Phase 4: Verification

- [x] 4.1 Run unit tests (i.e. `auth.test.ts`, `vehicles-crud.test.ts`) to ensure onboarding modifications and dummy creations do not break existing logic.
- [x] 4.2 Manually verify the UX behavior across a new Tenant Registration flow.
